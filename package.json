{
  "name": "portfolio",
  "version": "2.0.0",
  "description": "Lightweight static portfolio website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "deploy": "gh-pages -d public"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-handlebars": "^7.1.3"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "prettier": "^3.1.0",
    "gh-pages": "^6.1.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
